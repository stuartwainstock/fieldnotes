# 18 · Seed initial principles/values

**Phase:** 3 — Build & seed the Eleanor Leftwich instance
**Status:** Not started
**Depends on:** 13, 14
**Blocks:** —

## Context

Unlike the glossary and decision entries, you don't have this content yet — it has to come from actually working with Nicola and Kendall. This ticket is really "start capturing," not "finish capturing." Per the original CLAUDE.md content-authoring principles: a principle without `tension` is a platitude, so resist the urge to write generic values-statement filler.

## Acceptance criteria

- [ ] At least 2-3 seed principles captured from what you already picked up during the interview process — e.g. something around the founder's stated ~90% full-price sell-through target as a quality bar, or her explicit tension between high-touch client relationships and scaling
- [ ] Each has a real `tension` field, not just an `elaboration` — what does this principle conflict with in practice?
- [ ] `confidence` set honestly — most of these should probably be `evolving` or `experimental` at first, since you're inferring them rather than hearing them stated as settled team doctrine
- [ ] Ongoing: add a principle whenever something said in a 1:1 or meeting sounds like "how we do things here"

## Relevant files

- Sanity Studio, `principle` document type

## Notes

This is the one ticket in Phase 3 that's never really "done" — treat the acceptance criteria as a starting bar, not a completion state.
